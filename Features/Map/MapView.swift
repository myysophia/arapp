import SwiftUI

struct MapView: View {
    var body: some View {
        FeatureScaffoldView(
            title: "地图",
            subtitle: "热力层、模型点详情和区域差异会在这里承接。",
            primaryActionTitle: "查看 Onboarding"
        )
    }
}
