import SwiftUI

struct AlertsView: View {
    var body: some View {
        FeatureScaffoldView(
            title: "提醒",
            subtitle: "阈值、静默时段和最近记录会在这里承接。",
            primaryActionTitle: "打开登录页"
        )
    }
}
