import SwiftUI

struct TodayView: View {
    var body: some View {
        FeatureScaffoldView(
            title: "今日",
            subtitle: "风险总览、趋势和行动建议会从这里进入。",
            primaryActionTitle: "打开登录页"
        )
    }
}
